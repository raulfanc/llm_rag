1. using Streamlit to create simple UI for interaction.

```terminal
pip install streamlit
```

2. cd to the directory and then run streamlit app

```terminal
cd src/streamlit
streamlit run app.py
```

